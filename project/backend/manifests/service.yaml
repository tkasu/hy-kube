apiVersion: v1
kind: Service
metadata:
  name: hy-kube-backend-svc
spec:
  type: ClusterIP
  selector:
    app: hy-kube-backend
  ports:
    - name: http
      protocol: TCP
      port: 2345
      targetPort: 3000