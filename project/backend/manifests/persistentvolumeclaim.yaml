apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: hy-kube-project
  name: project-claim
spec:
  storageClassName: standard
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 100Mi